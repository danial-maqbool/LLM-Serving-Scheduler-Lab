# Start Here

This repository is prepared as a research-engineering starter for a deeper GPT-6 Astra Pro implementation pass.

## Immediate tasks for the next agent

1. Read `README.md`, `docs/PROJECT_SPEC.md`, `docs/METHODOLOGY.md`, and `docs/EXPERIMENT_PLAN.md` first.
2. Preserve the current simulator as a tested baseline.
3. Replace the single hard-coded timing equation with pluggable cost models.
4. Add trace import/export and multi-seed experiment sweeps.
5. Add static plotting and statistical analysis.
6. Add stronger scheduler invariants and adversarial tests.
7. Add saturation, burst, head-of-line, fairness, and sensitivity experiments.
8. Generate reproducible results and `REPORT.md`.
9. Keep claims explicitly separated into simulated vs measured.
10. Push after each coherent implementation phase.

## Important constraint

Do not turn this into a web application or dashboard. Static figures and command-line tooling are preferred.
